name: boltz2_env
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch==2.4.0
    - torchvision==0.19.0
    - pytorch-lightning ==2.5.0
    - boltz
    - py3Dmol
    - numpy
    - pandas
    - matplotlib
    - seaborn
    - pyyaml
    - ipython
    - ipywidgets